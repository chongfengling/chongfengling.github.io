<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://chongfengling.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://chongfengling.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-04-22T17:58:13+00:00</updated><id>https://chongfengling.github.io/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry xml:lang="zh"><title type="html">CUDA GEMM: Intro &amp;amp; Naive Implementation</title><link href="https://chongfengling.github.io/blog/2026/gemm-0/" rel="alternate" type="text/html" title="CUDA GEMM: Intro &amp;amp; Naive Implementation"/><published>2026-04-22T00:00:00+00:00</published><updated>2026-04-22T00:00:00+00:00</updated><id>https://chongfengling.github.io/blog/2026/gemm-0</id><content type="html" xml:base="https://chongfengling.github.io/blog/2026/gemm-0/"><![CDATA[<p><a href="https://zhuanlan.zhihu.com/p/2030433294089720009" target="_blank">中文版本请访问知乎 Chinese Version Here</a> <i class="fa-solid fa-arrow-up-right-from-square fa-xs"></i></p> <h1 id="1-引言-introduction">1. 引言 (Introduction)</h1> <p><strong>什么是 GEMM</strong></p> <ul> <li>通用矩阵乘法 (General Matrix Multiply, GEMM) 运算定义为 $C = A \times B$。给定矩阵 $A \in \mathbb{R}^{M \times K}$ 与 $B \in \mathbb{R}^{K \times N}$，计算目标矩阵 $C \in \mathbb{R}^{M \times N}$。</li> </ul> <p><strong>为何优化 GEMM</strong></p> <ul> <li>手写并逐级优化 GEMM 核函数，来深入理解底层硬件架构（内存层级、计算单元、指令调度）。</li> </ul> <p><strong>目标</strong></p> <ul> <li>实现并逐步优化单精度浮点 (FP 32) GEMM 核函数。目标将算法从内存受限 (Memory-Bound) 的基础实现，优化至接近 NVIDIA RTX 5080 (Blackwell) 硬件极限的高利用率架构。</li> </ul> <h2 id="性能度量体系-measurement">性能度量体系 (Measurement)</h2> <h3 id="总浮点运算量-total-flops">总浮点运算量 (Total FLOPs)</h3> <p>针对输出矩阵 $C$ 中的每一个元素，需执行 $K$ 次乘法与 $K$ 次加法（在 GPU 中通常融合成 FMA 指令）。全局计算负载计算如下：</p> \[Total\ FLOPs = 2MNK\] <h3 id="实际吞吐量-achieved-tflops">实际吞吐量 (Achieved TFLOPS)</h3> <p>核函数执行期间的实际计算性能：</p> \[Achieved\ TFLOPS = \frac{2MNK}{Execution\ Time\ (s) \times 10^{12}}\] <h3 id="算术强度与-roofline-模型-arithmetic-intensity">算术强度与 Roofline 模型 (Arithmetic Intensity)</h3> <p>算术强度定义为计算量与内存数据传输量的比值，用于判断算子是计算受限还是内存受限：</p> \[Arithmetic\ Intensity = \frac{Total\ FLOPs}{Total\ Memory\ Traffic\ (bytes)}\] <h3 id="硬件参数与机器平衡点-rtx-5080">硬件参数与机器平衡点 (RTX 5080)</h3> <ul> <li>理论峰值算力 (FP 32)：约 56.28 TFLOPS</li> <li>峰值显存带宽 (GDDR 7)：约 960 GB/s</li> </ul> <p>计算该硬件的机器平衡点 (Machine Balance)：</p> \[Machine\ Balance = \frac{Peak\ Compute}{Peak\ Bandwidth} = \frac{56280\ GFLOPs/s}{960\ GB/s} \approx 58.625\ FLOPs/Byte\] <ul> <li>算子的算术强度低于 58.625 FLOPs/Byte 时，可以认为 kernel 是 Memory Bound。</li> </ul> <hr/> <h1 id="2-基础实现-naive-gemm">2 基础实现 (Naive GEMM)</h1> <h2 id="21-核心思想">2.1 核心思想</h2> <p>采用最直接的映射策略。每个 CUDA Thread 负责计算输出矩阵 $C$ 中的一个元素。最小的可操控单元对应最小的矩阵元素。</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">__global__</span> <span class="kt">void</span> <span class="nf">gemm_naive_kernel</span><span class="p">(</span><span class="k">const</span> <span class="kt">float</span><span class="o">*</span> <span class="n">A</span><span class="p">,</span> <span class="k">const</span> <span class="kt">float</span><span class="o">*</span> <span class="n">B</span><span class="p">,</span> <span class="kt">float</span><span class="o">*</span> <span class="n">C</span><span class="p">,</span> <span class="kt">int</span> <span class="n">M</span><span class="p">,</span> <span class="kt">int</span> <span class="n">N</span><span class="p">,</span> <span class="kt">int</span> <span class="n">K</span><span class="p">)</span> <span class="p">{</span>
    <span class="c1">// 映射 2D 线程网格/块索引至矩阵的行与列</span>
    <span class="kt">int</span> <span class="n">row</span> <span class="o">=</span> <span class="n">blockIdx</span><span class="p">.</span><span class="n">y</span> <span class="o">*</span> <span class="n">blockDim</span><span class="p">.</span><span class="n">y</span> <span class="o">+</span> <span class="n">threadIdx</span><span class="p">.</span><span class="n">y</span><span class="p">;</span>
    <span class="kt">int</span> <span class="n">col</span> <span class="o">=</span> <span class="n">blockIdx</span><span class="p">.</span><span class="n">x</span> <span class="o">*</span> <span class="n">blockDim</span><span class="p">.</span><span class="n">x</span> <span class="o">+</span> <span class="n">threadIdx</span><span class="p">.</span><span class="n">x</span><span class="p">;</span>

    <span class="c1">// 边界检查，防止非对齐维度的越界访问</span>
    <span class="k">if</span> <span class="p">(</span><span class="n">row</span> <span class="o">&lt;</span> <span class="n">M</span> <span class="o">&amp;&amp;</span> <span class="n">col</span> <span class="o">&lt;</span> <span class="n">N</span><span class="p">)</span> <span class="p">{</span>
        <span class="kt">float</span> <span class="n">sum</span> <span class="o">=</span> <span class="mf">0.0f</span><span class="p">;</span>
        <span class="c1">// 计算 A 的第 row 行与 B 的第 col 列的内积</span>
        <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">K</span><span class="p">;</span> <span class="o">++</span><span class="n">i</span><span class="p">)</span> <span class="p">{</span>
            <span class="n">sum</span> <span class="o">+=</span> <span class="n">A</span><span class="p">[</span><span class="n">row</span> <span class="o">*</span> <span class="n">K</span> <span class="o">+</span> <span class="n">i</span><span class="p">]</span> <span class="o">*</span> <span class="n">B</span><span class="p">[</span><span class="n">i</span> <span class="o">*</span> <span class="n">N</span> <span class="o">+</span> <span class="n">col</span><span class="p">];</span>
        <span class="p">}</span>
        <span class="c1">// 将结果写回全局内存</span>
        <span class="n">C</span><span class="p">[</span><span class="n">row</span> <span class="o">*</span> <span class="n">N</span> <span class="o">+</span> <span class="n">col</span><span class="p">]</span> <span class="o">=</span> <span class="n">sum</span><span class="p">;</span>
    <span class="p">}</span>
<span class="p">}</span>

<span class="kt">void</span> <span class="nf">launch_gemm_naive</span><span class="p">(</span><span class="k">const</span> <span class="kt">float</span><span class="o">*</span> <span class="n">A</span><span class="p">,</span> <span class="k">const</span> <span class="kt">float</span><span class="o">*</span> <span class="n">B</span><span class="p">,</span> <span class="kt">float</span><span class="o">*</span> <span class="n">C</span><span class="p">,</span> <span class="kt">int</span> <span class="n">M</span><span class="p">,</span> <span class="kt">int</span> <span class="n">N</span><span class="p">,</span> <span class="kt">int</span> <span class="n">K</span><span class="p">)</span> <span class="p">{</span>
    <span class="c1">// 设定标准的 16x16 线程块</span>
    <span class="n">dim3</span> <span class="n">block</span><span class="p">(</span><span class="mi">16</span><span class="p">,</span> <span class="mi">16</span><span class="p">);</span>
    <span class="c1">// 向上取整计算 Grid 维度</span>
    <span class="n">dim3</span> <span class="n">grid</span><span class="p">((</span><span class="n">N</span> <span class="o">+</span> <span class="n">block</span><span class="p">.</span><span class="n">x</span> <span class="o">-</span> <span class="mi">1</span><span class="p">)</span> <span class="o">/</span> <span class="n">block</span><span class="p">.</span><span class="n">x</span><span class="p">,</span> <span class="p">(</span><span class="n">M</span> <span class="o">+</span> <span class="n">block</span><span class="p">.</span><span class="n">y</span> <span class="o">-</span> <span class="mi">1</span><span class="p">)</span> <span class="o">/</span> <span class="n">block</span><span class="p">.</span><span class="n">y</span><span class="p">);</span>
    <span class="n">gemm_naive_kernel</span><span class="o">&lt;&lt;&lt;</span><span class="n">grid</span><span class="p">,</span> <span class="n">block</span><span class="o">&gt;&gt;&gt;</span><span class="p">(</span><span class="n">A</span><span class="p">,</span> <span class="n">B</span><span class="p">,</span> <span class="n">C</span><span class="p">,</span> <span class="n">M</span><span class="p">,</span> <span class="n">N</span><span class="p">,</span> <span class="n">K</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div> <h2 id="22-代码级内存映射解析">2.2 代码级内存映射解析</h2> <ol> <li><strong>多维索引与物理内存的映射</strong> 在 C/C++ 中，二维数组的物理内存排布为<strong>行优先 (Row-Major)</strong>。即列下标 <code class="language-plaintext highlighter-rouge">col</code> 是内存地址中最内层（连续）的变化维度。相邻两列 (<code class="language-plaintext highlighter-rouge">[row][col]</code> 与 <code class="language-plaintext highlighter-rouge">[row][col+1]</code>) 在物理内存中相邻。</li> <li><strong>线程维度绑定策略</strong> GPU 执行指令的最小调度单位是 <strong>Warp</strong>（包含 32 个线程）。在同一个 Warp 内，<code class="language-plaintext highlighter-rouge">threadIdx.x</code> 的编号是连续的（是最内层（连续）的变化维度）。为促成<strong>合并内存访问 (Coalesced Access)</strong>，必须将矩阵的连续内存维度映射到线程的连续编号上。因此代码逻辑设定为：矩阵的列索引 <code class="language-plaintext highlighter-rouge">col</code> 绑定至 <code class="language-plaintext highlighter-rouge">threadIdx.x</code>，行索引 <code class="language-plaintext highlighter-rouge">row</code> 绑定至 <code class="language-plaintext highlighter-rouge">threadIdx.y</code>。</li> <li><strong>Warp 内的访问行为理论判断</strong> <ul> <li>读取 <code class="language-plaintext highlighter-rouge">B[i * N + col]</code>：索引依赖 <code class="language-plaintext highlighter-rouge">col</code> (<code class="language-plaintext highlighter-rouge">threadIdx.x</code>)。Warp 内相邻线程读取连续的地址，形成连续访存。</li> <li>读取 <code class="language-plaintext highlighter-rouge">A[row * K + i]</code>：索引依赖 <code class="language-plaintext highlighter-rouge">row</code> (<code class="language-plaintext highlighter-rouge">threadIdx.y</code>)。若 Warp 内所有线程的 <code class="language-plaintext highlighter-rouge">threadIdx.y</code> 相同，则形成同址广播访存。</li> </ul> </li> </ol> <h2 id="23-profiling-数据量化验证">2.3 Profiling 数据量化验证</h2> <h3 id="算术强度评估">算术强度评估</h3> <p>针对输出矩阵 $C$ 中的单一元素：</p> <ul> <li><strong>计算量</strong>：执行 $K$ 次乘法与 $K$ 次加法，总计 $2K$ FLOPs。</li> <li><strong>访存量</strong>：全局内存读取矩阵 A 的一行（$K$ 个 float）与矩阵 B 的一列（$K$ 个 float），总计读取 $2K$ 个浮点数。数据传输量为 $2K \times 4$ bytes = $8K$ bytes。</li> <li><strong>算术强度</strong>：$2K / 8K = \mathbf{0.25\ FLOPs/Byte}$。 与 RTX 5080 的机器平衡点 ($58.625$) 相比，该算法严重受限于内存带宽。</li> </ul> <h3 id="底层物理内存搬运分析">底层物理内存搬运分析</h3> <p><strong>传输原则基准</strong></p> <ul> <li><strong>Sector（扇区）</strong>：GPU 显存（DRAM/L 2 Cache）与计算核心（SM/L 1 Cache）之间数据传输的最小不可分割物理单位，固定为 <strong>32 字节</strong>。</li> <li>在理想（<strong>合并访存</strong>）状态下，一个 warp 的 32 个线程访问连续的物理内存地址地址，且地址上的信息都被使用。假设数据类型为 <code class="language-plaintext highlighter-rouge">float</code>（4 字节）。一次单次全局读取命令需要 32 线程 * 4 字节 = 128 字节，即 4 个 连续 sectors.</li> </ul> <p><strong>访存行为微观拆解 (基于 <code class="language-plaintext highlighter-rouge">dim3 block(16, 16)</code>)</strong> 由于 Block 宽度为 16，无法达到合并访存状态。</p> <ul> <li>读取 B 矩阵 (<code class="language-plaintext highlighter-rouge">B[i * N + col]</code>)： <ul> <li>行为：前 16 线程与后 16 线程请求完全重复的 <code class="language-plaintext highlighter-rouge">col</code> 范围 (0~15)。</li> <li>有效数据：16 个不重复 float，计 64 字节。</li> <li>物理执行：触发 <strong>2 个连续扇区</strong>。</li> </ul> </li> <li>读取 A 矩阵 (<code class="language-plaintext highlighter-rouge">A[row * K + i]</code>)： <ul> <li>行为：前 16 线程与后 16 线程分别请求相隔一整行（K 个元素）的物理地址。</li> <li>有效数据：2 个不重复 float，计 8 字节。</li> <li>物理执行：因地址跨度过大，强制触发 <strong>2 个独立扇区</strong>。</li> </ul> </li> </ul> <p><strong>NCU 硬件计数器印证</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>gemm_naive_kernel<span class="o">(</span>const float <span class="k">*</span>, const float <span class="k">*</span>, float <span class="k">*</span>, int, int, int<span class="o">)</span> <span class="o">(</span>128, 128, 1<span class="o">)</span>x<span class="o">(</span>16, 16, 1<span class="o">)</span>, Context 1, Stream 7, Device 0, CC 12.0
  Section: Command line profiler metrics
  <span class="nt">-----------------------------------------------</span> <span class="nt">-----------</span> <span class="nt">-------------</span>
  Metric Name                                     Metric Unit  Metric Value
  <span class="nt">-----------------------------------------------</span> <span class="nt">-----------</span> <span class="nt">-------------</span>
  l1tex__t_requests_pipe_lsu_mem_global_op_ld.sum               536,870,912
  l1tex__t_sectors_pipe_lsu_mem_global_op_ld.sum       sector 1,073,741,824
  <span class="nt">-----------------------------------------------</span> <span class="nt">-----------</span> <span class="nt">-------------</span>
</code></pre></div></div> <p><strong>指标一：扇区请求比 (Sectors per Request)</strong></p> <ul> <li>理论计算：循环内发出的 2 次请求（读 A + 读 B），共计触发 4 个扇区。比值为 $4/2 = \mathbf{2.0\ 扇区/请求}$。</li> <li>实测数据：<code class="language-plaintext highlighter-rouge">1,073,741,824 / 536,870,912 = 2.0</code>。数据吻合。</li> </ul> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Section: Memory Workload Analysis Tables
OPT   Est. Speedup: 42.18%                                                                                          
      The memory access pattern <span class="k">for </span>global loads from L1TEX might not be optimal. On average, only 18.0 of the 32   
      bytes transmitted per sector are utilized by each thread. This could possibly be caused by a stride between   
      threads. Check the Source Counters section <span class="k">for </span>uncoalesced global loads.  
</code></pre></div></div> <p><strong>指标二：平均扇区有效利用率 (bytes per Sector)</strong></p> <ul> <li>单次循环有效利用数据：读 B (64 B) + 读 A (8 B) = 72 bytes。</li> <li>单次循环实际搬运数据：4 个扇区 $\times$ 32 B = 128 bytes。</li> <li>总共利用了 4 个扇区，平均每个扇区的 32 个 bytes 中，有效数据量为 18 bytes。</li> <li>实测数据： <code class="language-plaintext highlighter-rouge">only 18.0 of the 32 bytes</code> 。数据吻合。</li> </ul> <h2 id="24-优化点物理对齐">2.4 优化点：物理对齐</h2> <h3 id="修改-blockdimx-为-32-的倍数">修改 <code class="language-plaintext highlighter-rouge">blockDim.x</code> 为 32 的倍数</h3> <p><strong>内存访问分析</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Section: Memory Workload Analysis Tables
OPT   Est. Speedup: 17.21%                                                                                          
      The memory access pattern <span class="k">for </span>global loads from L1TEX might not be optimal. On average, only 26.4 of the 32   
      bytes transmitted per sector are utilized by each thread. This could possibly be caused by a stride between   
      threads. Check the Source Counters section <span class="k">for </span>uncoalesced global loads. 
</code></pre></div></div> <ul> <li><strong>取矩阵 B (<code class="language-plaintext highlighter-rouge">B[i * N + col]</code>)：100% 满载合并访存</strong> <ul> <li>行为：Warp 内 32 个线程请求连续的 32 个列地址（<code class="language-plaintext highlighter-rouge">col</code> 连续）。</li> <li>有效数据量：32 个独立 <code class="language-plaintext highlighter-rouge">float</code>，共计 128 字节。</li> <li>物理行为：数据量精确覆盖 4 个对齐的 32 字节扇区。LSU 触发单次 128 字节的合并事务，平均每扇区利用率达到 32/32 字节的物理极值。</li> </ul> </li> <li><strong>读取矩阵 A (<code class="language-plaintext highlighter-rouge">A[row * K + i]</code>)：同址完美广播</strong> <ul> <li>行为：Warp 内 32 个线程计算处于同一逻辑行（<code class="language-plaintext highlighter-rouge">row</code> 相同），请求完全相同的单一物理地址。</li> <li>有效数据量：1 个 <code class="language-plaintext highlighter-rouge">float</code>，共计 4 字节。</li> <li>物理行为：LSU 触发 1 个扇区 (32 字节) 请求。数据抵达 L 1 Cache 后，触发 L 1 内部同周期广播（Broadcast）机制，将单一数据同时分发至 32 个 SIMT 通道，消除跨行离散惩罚。</li> </ul> </li> <li><strong>平均扇区有效利用率 (bytes per Sector)</strong> <ul> <li>单次循环有效利用数据：读 B (128 B) + 读 A (4B) = 132 bytes。</li> <li>单次循环实际搬运数据：5个扇区 $\times$ 32 B = 160 bytes。</li> <li>总共利用了 4 个扇区，平均每个扇区的 32 个 bytes 中，有效数据量为 26.5 bytes。</li> <li>实测数据： <code class="language-plaintext highlighter-rouge">only 26.4 of the 32 bytes</code> 。数据吻合。</li> </ul> </li> </ul> <p><strong>算术强度分析</strong></p> <ul> <li>仅解决了“单次请求内部的物理利用率”问题，未解决“跨请求的数据复用”问题。每次执行乘加运算（FMA），线程依然需要去全局内存 (Global Memory) 发起真实的物理读取。同一块数据矩阵依然被不同的 Thread Block 重复拉取无数次。无法提高算术强度</li> </ul> <h2 id="25-performance">2.5 Performance</h2> <table> <thead> <tr> <th><strong>M</strong></th> <th><strong>N</strong></th> <th><strong>K</strong></th> <th><strong>Kernel</strong></th> <th><strong>Time (ms)</strong></th> <th><strong>TFLOPS</strong></th> </tr> </thead> <tbody> <tr> <td>2048</td> <td>2048</td> <td>2048</td> <td><strong>NAIVE_16 X 16</strong></td> <td>5.1235</td> <td>3.35</td> </tr> <tr> <td>2048</td> <td>2048</td> <td>2048</td> <td><strong>NAIVE_32 X 16</strong></td> <td>4.9678</td> <td>3.46</td> </tr> <tr> <td>2048</td> <td>2048</td> <td>2048</td> <td><strong>CUBLAS</strong></td> <td>0.5127</td> <td>33.51</td> </tr> </tbody> </table> <ul> <li>$Total FLOPS =2 * 2048^{3} \approx 17.18\;GFLOPS$ <h2 id="26-总结">2.6 总结</h2> </li> </ul> <pre><code class="language-txt">[ 计算核心 (CUDA Cores) ]
       |    ^
       |    | 4 Bytes (单线程单次请求 1 个 float)
       v    |
+-----------------------+
|  寄存器 (Registers)   |  &lt;-- 累加操作 (sum += ...) 发生在此处
+-----------------------+
            ^
            | L1 命中则直接返回 / 同周期广播 (Broadcast)
            |
+-----------------------+
|    L1 Cache / TEX     |  &lt;-- 位于 SM 内部，管理单位为 Cache Line (128 Bytes)
+-----------------------+
            ^
            | 32 Bytes (1 个 Sector，物理传输的最小不可分割单位)
            | LSU (Load/Store Unit) 在此层级发起物理请求
            |
+-----------------------+
|       L2 Cache        |  &lt;-- 所有 SM 共享
+-----------------------+
            ^
            | 内存事务 (Memory Transaction：32 / 64 / 128 Bytes)
            |
+-----------------------+
|  Global Memory (DRAM) |  &lt;-- 矩阵 A、矩阵 B、矩阵 C 所在位置
+-----------------------+
</code></pre> <p>Naive GEMM 架构被死死钉在性能底谷的核心原因，可以归结为一个致命的物理缺陷：<strong>极低的算术强度引发了全局内存的灾难性冗余读取。</strong></p> <h4 id="1-缺陷剖析on-级别的冗余拉取">1. 缺陷剖析：O(N) 级别的冗余拉取</h4> <p>在 Naive 实现中，计算逻辑是“以线程为孤岛”的。计算 Ci,j​ 的线程需要读取矩阵 A 的第 i 行；而计算其右侧相邻元素 Ci,j+1​ 的线程，<strong>再次独立发起</strong>了对矩阵 A 第 i 行的全局内存读取请求。</p> <p>在物理层面上，矩阵 A 的同一行、矩阵 B 的同一列，被数以万计的不同线程反复从高延迟的 Global Memory 中重复搬运。对于 N×N 的方阵，<strong>Global Memory 中的每一个浮点数，都被物理拉取了整整 N 次</strong>。在 N=2048 的测试中，硬件显存总线承担了理论值 2048 倍的数据搬运重压，彻底击穿了 GDDR 7 的带宽上限。</p>]]></content><author><name></name></author><category term="CUDA"/><category term="kernel"/><category term="gemm"/><category term="ncu"/><category term="gemini"/><summary type="html"><![CDATA[中文版本请访问知乎 Chinese Version Here 1. 引言 (Introduction)]]></summary></entry><entry><title type="html">CUDA-Softmax (Draft)</title><link href="https://chongfengling.github.io/blog/2026/draft/" rel="alternate" type="text/html" title="CUDA-Softmax (Draft)"/><published>2026-03-15T00:00:00+00:00</published><updated>2026-03-15T00:00:00+00:00</updated><id>https://chongfengling.github.io/blog/2026/draft</id><content type="html" xml:base="https://chongfengling.github.io/blog/2026/draft/"><![CDATA[<hr/> <h1 id="analysis-of-three-cuda-softmax-implementations">Analysis of Three CUDA Softmax Implementations</h1> <p>This document provides an overview and analysis of three distinct CUDA implementations for the softmax function: <strong>Naive</strong>, <strong>Warp-Optimized</strong>, and <strong>Online</strong>. Each version represents a different level of optimization, targeting reduced memory latency, improved parallelism, and better numerical stability.</p> <hr/> <h2 id="1-naive-softmax-softmax_naivecu">1. Naive Softmax (<code class="language-plaintext highlighter-rouge">softmax_naive.cu</code>)</h2> <p>This version implements a straightforward, multi-pass algorithm using shared memory for block-level reductions. It serves as a baseline for understanding the fundamental steps of a parallel softmax.</p> <h3 id="core-idea">Core Idea</h3> <p>The implementation breaks the softmax calculation into three distinct passes over the data for each row:</p> <ul> <li><strong>Find Max</strong>: Find the maximum value in the row for numerical stability.</li> <li><strong>Calculate Sum</strong>: Compute $exp(x - max)$ for each element and sum them up.</li> <li><strong>Normalize</strong>: Divide each $exp(x - max)$ value by the total sum.</li> </ul> <p>Reductions (for finding the max and the sum) are performed across the entire CUDA block using shared memory.</p> <h3 id="core-functions">Core Functions</h3> <p>**<code class="language-plaintext highlighter-rouge">block_reduce_max(float* sdata)</code> &amp; <code class="language-plaintext highlighter-rouge">block_reduce_sum(float* sdata)**</code> These are helper functions that perform a parallel reduction on data stored in the <code class="language-plaintext highlighter-rouge">sdata</code> shared memory array. They use a classic tree-based reduction pattern.</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// --- Helper: 树形归约求和 ---</span>
<span class="n">__device__</span> <span class="kt">void</span> <span class="nf">block_reduce_sum</span><span class="p">(</span><span class="kt">float</span><span class="o">*</span> <span class="n">sdata</span><span class="p">)</span> <span class="p">{</span>
    <span class="kt">unsigned</span> <span class="kt">int</span> <span class="n">tid</span> <span class="o">=</span> <span class="n">threadIdx</span><span class="p">.</span><span class="n">x</span><span class="p">;</span>
    <span class="c1">// The loop halves the number of active threads in each iteration</span>
    <span class="k">for</span> <span class="p">(</span><span class="kt">unsigned</span> <span class="kt">int</span> <span class="n">s</span> <span class="o">=</span> <span class="n">blockDim</span><span class="p">.</span><span class="n">x</span> <span class="o">/</span> <span class="mi">2</span><span class="p">;</span> <span class="n">s</span> <span class="o">&gt;</span> <span class="mi">0</span><span class="p">;</span> <span class="n">s</span> <span class="o">&gt;&gt;=</span> <span class="mi">1</span><span class="p">)</span> <span class="p">{</span>
        <span class="k">if</span> <span class="p">(</span><span class="n">tid</span> <span class="o">&lt;</span> <span class="n">s</span><span class="p">)</span> <span class="p">{</span>
            <span class="c1">// The first half of threads aggregate data from the second half</span>
            <span class="n">sdata</span><span class="p">[</span><span class="n">tid</span><span class="p">]</span> <span class="o">+=</span> <span class="n">sdata</span><span class="p">[</span><span class="n">tid</span> <span class="o">+</span> <span class="n">s</span><span class="p">];</span>
        <span class="p">}</span>
        <span class="c1">// A barrier is crucial to ensure all threads in the block</span>
        <span class="c1">// complete the current reduction step before proceeding to the next.</span>
        <span class="n">__syncthreads</span><span class="p">();</span>
    <span class="p">}</span>
<span class="p">}</span>

</code></pre></div></div> <h3 id="key-characteristics">Key Characteristics</h3> <ul> <li>**Dependency on <code class="language-plaintext highlighter-rouge">__syncthreads()**</code>: Synchronization is required at every step of the reduction, which can introduce latency.</li> <li><strong>Shared Memory Bottleneck</strong>: All data exchange happens through shared memory, which is slower than register-to-register communication.</li> <li><strong>Multiple Passes</strong>: The kernel reads the input data three times (once in the max pass, once in the sum pass, and once in the normalization pass, although the last two are combined).</li> </ul> <hr/> <h2 id="2-warp-optimized-softmax-softmax_warpcu">2. Warp-Optimized Softmax (<code class="language-plaintext highlighter-rouge">softmax_warp.cu</code>)</h2> <p>This version optimizes the reduction process by leveraging warp-level primitives, which allow for direct communication between threads within a warp (a group of 32 threads) without using shared memory.</p> <h3 id="core-idea-1">Core Idea</h3> <p>The key change is to replace the block-wide <code class="language-plaintext highlighter-rouge">__syncthreads()</code>-based reduction with a more efficient two-level reduction strategy:</p> <ol> <li><strong>Intra-Warp Reduction</strong>: A fast reduction is performed within each warp using <code class="language-plaintext highlighter-rouge">__shfl_down_sync</code>. The result is held in the registers of the warp’s “leader” thread (lane 0).</li> <li><strong>Inter-Warp Reduction</strong>: The leader of each warp writes its partial result to shared memory. Then, a single warp performs a final reduction on these values from shared memory.</li> </ol> <p>This significantly reduces the reliance on <code class="language-plaintext highlighter-rouge">__syncthreads()</code> and shared memory traffic.</p> <h3 id="core-functions-1">Core Functions</h3> <p>**<code class="language-plaintext highlighter-rouge">warpReduceMax(float val)</code> &amp; <code class="language-plaintext highlighter-rouge">warpReduceSum(float val)**</code> These functions perform a reduction across the 32 threads of a warp.</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">static</span> <span class="n">__inline__</span> <span class="n">__device__</span> <span class="kt">float</span> <span class="nf">warpReduceMax</span><span class="p">(</span><span class="kt">float</span> <span class="n">val</span><span class="p">)</span> <span class="p">{</span>
    <span class="c1">// This loop performs a tree-based reduction within a warp.</span>
    <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">offset</span> <span class="o">=</span> <span class="n">WARP_SIZE</span> <span class="o">/</span> <span class="mi">2</span><span class="p">;</span> <span class="n">offset</span> <span class="o">&gt;</span> <span class="mi">0</span><span class="p">;</span> <span class="n">offset</span> <span class="o">/=</span> <span class="mi">2</span><span class="p">)</span> <span class="p">{</span>
        <span class="c1">// __shfl_down_sync fetches a value from another thread in the same warp.</span>
        <span class="c1">// It's a direct register-to-register communication, avoiding shared memory.</span>
        <span class="c1">// The first argument (mask) determines which threads participate. 0xffffffff means all 32 threads.</span>
        <span class="kt">float</span> <span class="n">other</span> <span class="o">=</span> <span class="n">__shfl_down_sync</span><span class="p">(</span><span class="mh">0xffffffff</span><span class="p">,</span> <span class="n">val</span><span class="p">,</span> <span class="n">offset</span><span class="p">);</span>
        <span class="n">val</span> <span class="o">=</span> <span class="n">fmaxf</span><span class="p">(</span><span class="n">val</span><span class="p">,</span> <span class="n">other</span><span class="p">);</span>
    <span class="p">}</span>
    <span class="c1">// After the loop, the thread with laneId 0 holds the maximum value for the warp.</span>
    <span class="k">return</span> <span class="n">val</span><span class="p">;</span>
<span class="p">}</span>

</code></pre></div></div> <h3 id="key-changes-from-naive">Key Changes from Naive</h3> <ul> <li><strong><code class="language-plaintext highlighter-rouge">__shfl_down_sync</code></strong>: This is the core of the optimization. It allows a thread to read a register from another thread in the same warp, identified by <code class="language-plaintext highlighter-rouge">current_lane + offset</code>. This is much faster than writing to and reading from shared memory.</li> <li>**Reduced <code class="language-plaintext highlighter-rouge">__syncthreads()**</code>: Synchronization barriers are now only needed between major steps (e.g., after writing to shared memory, before the final reduction), not within the warp reduction itself.</li> <li><strong>Smaller Shared Memory</strong>: Shared memory is only needed to store the partial result from each warp, so its size is reduced from <code class="language-plaintext highlighter-rouge">BLOCK_SIZE</code> to <code class="language-plaintext highlighter-rouge">BLOCK_SIZE / WARP_SIZE</code>.</li> </ul> <hr/> <h2 id="3-online-softmax-softmax_onlinecu">3. Online Softmax (<code class="language-plaintext highlighter-rouge">softmax_online.cu</code>)</h2> <p>This version introduces a significant algorithmic optimization. It computes the max and sum in a single pass over the input data, drastically reducing memory bandwidth requirements. This is based on a numerically stable “online” algorithm.</p> <h3 id="core-idea-2">Core Idea</h3> <p>Instead of finding the global max first and then computing the sum, this algorithm maintains a running <code class="language-plaintext highlighter-rouge">max_val</code> and <code class="language-plaintext highlighter-rouge">sum</code> as it iterates through the data. When a new element $x$ is processed, the current statistics are updated. If $x$ is larger than the current <code class="language-plaintext highlighter-rouge">max_val</code>, the existing sum must be rescaled to be relative to the new maximum.</p> <h3 id="core-functions--data-structures">Core Functions &amp; Data Structures</h3> <p><strong><code class="language-plaintext highlighter-rouge">struct OnlineStat</code></strong> A simple struct is used to bundle the running max and sum together.</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">struct</span> <span class="nc">__align__</span><span class="p">(</span><span class="mi">8</span><span class="p">)</span> <span class="n">OnlineStat</span> <span class="p">{</span>
    <span class="kt">float</span> <span class="n">max_val</span><span class="p">;</span>
    <span class="kt">float</span> <span class="n">sum</span><span class="p">;</span>
<span class="p">};</span>

</code></pre></div></div> <p><strong><code class="language-plaintext highlighter-rouge">combine_stat(OnlineStat a, OnlineStat b)</code></strong> This is the mathematical core of the online algorithm. It defines how to merge two <code class="language-plaintext highlighter-rouge">OnlineStat</code> instances.</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">__inline__</span> <span class="n">__device__</span> <span class="n">OnlineStat</span> <span class="nf">combine_stat</span><span class="p">(</span><span class="n">OnlineStat</span> <span class="n">a</span><span class="p">,</span> <span class="n">OnlineStat</span> <span class="n">b</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">OnlineStat</span> <span class="n">res</span><span class="p">;</span>
    
    <span class="c1">// The new max is simply the greater of the two.</span>
    <span class="n">res</span><span class="p">.</span><span class="n">max_val</span> <span class="o">=</span> <span class="n">fmaxf</span><span class="p">(</span><span class="n">a</span><span class="p">.</span><span class="n">max_val</span><span class="p">,</span> <span class="n">b</span><span class="p">.</span><span class="n">max_val</span><span class="p">);</span>

    <span class="c1">// The sums must be rescaled relative to the new overall max before being added.</span>
    <span class="c1">// This prevents numerical overflow/underflow.</span>
    <span class="n">res</span><span class="p">.</span><span class="n">sum</span> <span class="o">=</span> <span class="n">a</span><span class="p">.</span><span class="n">sum</span> <span class="o">*</span> <span class="n">expf</span><span class="p">(</span><span class="n">a</span><span class="p">.</span><span class="n">max_val</span> <span class="o">-</span> <span class="n">res</span><span class="p">.</span><span class="n">max_val</span><span class="p">)</span> <span class="o">+</span> <span class="n">b</span><span class="p">.</span><span class="n">sum</span> <span class="o">*</span> <span class="n">expf</span><span class="p">(</span><span class="n">b</span><span class="p">.</span><span class="n">max_val</span> <span class="o">-</span> <span class="n">res</span><span class="p">.</span><span class="n">max_val</span><span class="p">);</span>

    <span class="k">return</span> <span class="n">res</span><span class="p">;</span>
<span class="p">}</span>

</code></pre></div></div> <p><strong><code class="language-plaintext highlighter-rouge">warpReduceStat(OnlineStat val)</code></strong> This function is analogous to <code class="language-plaintext highlighter-rouge">warpReduceSum</code> but operates on the <code class="language-plaintext highlighter-rouge">OnlineStat</code> struct. Since <code class="language-plaintext highlighter-rouge">__shfl_down_sync</code> can only handle 32-bit types (like float), the <code class="language-plaintext highlighter-rouge">max_val</code> and <code class="language-plaintext highlighter-rouge">sum</code> members of the struct must be shuffled independently and then combined.</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">__inline__</span> <span class="n">__device__</span> <span class="n">OnlineStat</span> <span class="nf">warpReduceStat</span><span class="p">(</span><span class="n">OnlineStat</span> <span class="n">val</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">offset</span> <span class="o">=</span> <span class="n">WARP_SIZE</span> <span class="o">/</span> <span class="mi">2</span><span class="p">;</span> <span class="n">offset</span> <span class="o">&gt;</span> <span class="mi">0</span><span class="p">;</span> <span class="n">offset</span> <span class="o">/=</span> <span class="mi">2</span><span class="p">)</span> <span class="p">{</span>
        <span class="n">OnlineStat</span> <span class="n">other</span><span class="p">;</span>
        
        <span class="c1">// Shuffle the max and sum components separately.</span>
        <span class="n">other</span><span class="p">.</span><span class="n">max_val</span> <span class="o">=</span> <span class="n">__shfl_down_sync</span><span class="p">(</span><span class="mh">0xffffffff</span><span class="p">,</span> <span class="n">val</span><span class="p">.</span><span class="n">max_val</span><span class="p">,</span> <span class="n">offset</span><span class="p">);</span>
        <span class="n">other</span><span class="p">.</span><span class="n">sum</span> <span class="o">=</span> <span class="n">__shfl_down_sync</span><span class="p">(</span><span class="mh">0xffffffff</span><span class="p">,</span> <span class="n">val</span><span class="p">.</span><span class="n">sum</span><span class="p">,</span> <span class="n">offset</span><span class="p">);</span>

        <span class="c1">// Combine the local stat with the one from the other thread.</span>
        <span class="n">val</span> <span class="o">=</span> <span class="n">combine_stat</span><span class="p">(</span><span class="n">val</span><span class="p">,</span> <span class="n">other</span><span class="p">);</span>
    <span class="p">}</span>
    <span class="k">return</span> <span class="n">val</span><span class="p">;</span>
<span class="p">}</span>

</code></pre></div></div> <h3 id="key-changes-from-warp-optimized">Key Changes from Warp-Optimized</h3> <ul> <li><strong>Single Pass Reduction</strong>: The kernel reads the input data only once to compute both the final max and sum, significantly improving memory efficiency. A second pass is still required to perform the final normalization and write to output memory.</li> <li><strong>Algorithmic Complexity</strong>: The logic is more complex, requiring a careful update formula (<code class="language-plaintext highlighter-rouge">combine_stat</code>) to maintain numerical stability while processing elements sequentially.</li> <li><strong>Struct Manipulation</strong>: The reduction logic is adapted to handle a custom struct, demonstrating how to use warp shuffles for more complex data types by breaking them down into primitive components.</li> </ul> <hr/> <p>Here is the analysis of memory bandwidth for the three CUDA softmax implementations in English.</p> <hr/> <h2 id="memory-bandwidth-analysis-of-cuda-softmax">Memory Bandwidth Analysis of CUDA Softmax</h2> <p>In CUDA kernel optimization, memory bandwidth is often the primary bottleneck for element-wise or reduction operations like Softmax. This analysis quantifies the data movement between <strong>Global Memory</strong> and the GPU cores for a matrix of size $N \times D$ (where $N$ is the number of rows and $D$ is the number of elements per row), assuming <code class="language-plaintext highlighter-rouge">float</code> precision (4 bytes per element).</p> <h3 id="1-naive-softmax-the-multi-pass-model">1. Naive Softmax: The Multi-Pass Model</h3> <p>The <code class="language-plaintext highlighter-rouge">softmax_naive.cu</code> implementation follows a sequential logic that necessitates multiple traversals of the input data in global memory.</p> <ul> <li><strong>Read Operations</strong>: <ol> <li><strong>Find Max</strong>: Traverses the row to find the maximum value (Reads $D$ elements).</li> <li><strong>Calculate Sum</strong>: Traverses again to compute $\sum e^{x_i - max}$ (Reads $D$ elements).</li> <li><strong>Normalize</strong>: Reads the data a third time to perform the final division (Reads $D$ elements).</li> </ol> </li> <li><strong>Write Operations</strong>: Writes the final normalized results back to memory (Writes $D$ elements).</li> <li><strong>Total Traffic Formula</strong>: $Total_Bytes = N \times (3 \times D \times 4 + 1 \times D \times 4) = 16ND$ bytes.</li> </ul> <hr/> <h3 id="2-warp-optimized-softmax-instruction-vs-bandwidth">2. Warp-Optimized Softmax: Instruction vs. Bandwidth</h3> <p>While <code class="language-plaintext highlighter-rouge">softmax_warp.cu</code> significantly improves performance by using <strong>Warp Shuffle</strong> primitives to eliminate shared memory latency and synchronization barriers (<code class="language-plaintext highlighter-rouge">__syncthreads()</code>), its global memory footprint remains similar to the Naive version.</p> <ul> <li><strong>Traffic Characteristics</strong>: Unless the row data fits entirely within the L1/L2 cache, the kernel still typically performs separate passes to reduce the maximum and the sum.</li> <li><strong>Total Traffic Formula</strong>: Approximately $12ND$ to $16ND$ bytes.</li> <li><strong>Key Advantage</strong>: The speedup in this version comes from <strong>reduced instruction overhead</strong> and <strong>lower inter-thread communication latency</strong>, rather than a drastic reduction in total bandwidth consumption.</li> </ul> <hr/> <h3 id="3-online-softmax-the-one-pass-model">3. Online Softmax: The One-Pass Model</h3> <p>The <code class="language-plaintext highlighter-rouge">softmax_online.cu</code> implementation provides the most significant bandwidth optimization by utilizing an online algorithm to merge the “Find Max” and “Calculate Sum” steps into a single pass.</p> <ul> <li><strong>Read Operations</strong>: <ol> <li><strong>Reduction Pass</strong>: A single traversal reads each element once to compute both the running max and the rescaled sum simultaneously.</li> <li><strong>Normalization Pass</strong>: A second traversal reads the data to apply the final computed statistics.</li> </ol> </li> <li><strong>Write Operations</strong>: Writes the final results (Writes $D$ elements).</li> <li><strong>Total Traffic Formula</strong>: $Total_Bytes = N \times (2 \times D \times 4 + 1 \times D \times 4) = 12ND$ bytes.</li> <li><strong>Theoretical Gain</strong>: This represents a <strong>25% to 33% reduction</strong> in total memory traffic compared to standard multi-pass implementations.</li> </ul> <hr/> <h3 id="efficiency-comparison-summary">Efficiency Comparison Summary</h3> <table> <thead> <tr> <th>Implementation</th> <th>Global Memory Reads</th> <th>Global Memory Writes</th> <th>Theoretical Traffic (per element)</th> <th>Primary Bottleneck</th> </tr> </thead> <tbody> <tr> <td><strong>Naive</strong></td> <td>3</td> <td>1</td> <td>16 Bytes</td> <td>Sync Latency &amp; Bandwidth</td> </tr> <tr> <td><strong>Warp-Optimized</strong></td> <td>3</td> <td>1</td> <td>16 Bytes</td> <td>Memory Bandwidth</td> </tr> <tr> <td><strong>Online</strong></td> <td><strong>2</strong></td> <td>1</td> <td><strong>12 Bytes</strong></td> <td>ALU / Bandwidth Balance</td> </tr> </tbody> </table> <p>Would you like me to provide a benchmark script to measure the actual throughput (GB/s) of these implementations on your hardware?</p>]]></content><author><name></name></author><category term="tutorial"/><category term="cuda"/><summary type="html"><![CDATA[softmax]]></summary></entry></feed>